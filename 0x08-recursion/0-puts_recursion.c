#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - print a string followed by a new line
 * @s:string
 * Return: void
*/

void _puts_recursion(char *s)
{
	int i = 0;

	if (*(s + i) != NULL)
	{
		printf(*(s + i);
		i += 1;
	}
	else
	break;
}
