#include<stdio.h>
#include<string.h>
#include<stdlib.h>
/**
 * main - Entry Point
 *Return: always 0
*/
int main(void)
{
char a;
	putchar(a);
	for (a = 'a'; <= 'z'; a++)
	{
	putchar(a);
	}
return (0);
}
