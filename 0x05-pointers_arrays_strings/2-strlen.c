#include<stdio.h>
#include<stdlib.h>
#include<string.h>
/**
 * _strlen - give the lenght of a char
 * @s:parameter which is a char type
 *Return:always 0
*/

int _strlen(char *s)
{
int i;
i = strlen(s);
return (0);
}
