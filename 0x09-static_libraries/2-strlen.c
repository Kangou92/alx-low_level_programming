#include<stdio.h>
#include<stdlib.h>
#include<string.h>
/**
 * _strlen - give the lenght of a char
 * @s:parameter which is a char type
 *Return:the lengh of the string
*/

int _strlen(char *s)
{
int i;
i = strlen(s);
return (i);
}
