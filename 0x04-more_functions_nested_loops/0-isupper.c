#include "main.h"
#include<ctype.h>
#include<stdio.h>
/**
 * main - entry point
 * Return: always 0
 * @c: integer
*/

int _isupper(int c);
{
	if (c < 65 && c > 91)
	{
		return (0);
	}
	else
		return (1);
}
